import { auth } from "~/core/config/auth";
import { base } from "~/core/config/base";
import { mailer } from "~/core/config/mailer";
import { meta } from "~/core/config/meta";

export const configuration = {
  locale: "en",
  base,
  auth,
  mailer,
  meta,
};
